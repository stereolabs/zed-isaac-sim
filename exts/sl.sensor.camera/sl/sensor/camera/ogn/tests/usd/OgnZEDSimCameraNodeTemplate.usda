#usda 1.0
(
    doc ="""Generated from node description file OgnZEDSimCameraNode.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_sl_sensor_camera_OgnZEDSimCameraNode" (
        docs="""internal node used to stream images to the zed sdk"""
    )
    {
        token node:type = "sl.sensor.camera.OgnZEDSimCameraNode"
        int node:typeVersion = 3

        # 26 attributes
        custom bool inputs:applyZedSim2Real = false (
            docs="""Apply the Imatest-calibrated ZED Sim2Real camera model (MTF, vignetting, AE, bloom, AWB, tone, sharpening, sensor noise) to the frames before streaming"""
        )
        custom uint inputs:bitrate = 8000 (
            docs="""streaming bitrate (in Kbps). Only used for network transport layer mode (not IPC)"""
        )
        custom uint64 inputs:bufferSizeDepth = 0 (
            docs="""Size (in bytes) of the depth buffer"""
        )
        custom uint64 inputs:bufferSizeLeft = 0 (
            docs="""Size (in bytes) of the buffer (0 if the input is a texture)"""
        )
        custom uint64 inputs:bufferSizeRight = 0 (
            docs="""Size (in bytes) of the buffer (0 if the input is a texture)"""
        )
        custom uint inputs:chunkSize = 4096 (
            docs="""streaming chunk size (in bytes). Only used for network transport layer mode (not IPC)"""
        )
        custom uint64 inputs:dataPtrDepth = 0 (
            docs="""Pointer to the depth data (cuda device pointer, float32)"""
        )
        custom uint64 inputs:dataPtrLeft = 0 (
            docs="""Pointer to the raw data (cuda device pointer or host pointer)"""
        )
        custom uint64 inputs:dataPtrRight = 0 (
            docs="""Pointer to the raw data (cuda device pointer or host pointer)"""
        )
        custom uint inputs:execIn = 0 (
            docs="""Triggers execution"""
        )
        custom uint inputs:fps = 30 (
            docs="""frame rate"""
        )
        custom uint inputs:height = 600 (
            docs="""Camera stream height in pixels."""
        )
        custom vector3d inputs:linearAcceleration = (0.0, 0.0, 0.0) (
            docs="""imu acceleration"""
        )
        custom quatd inputs:orientation = (1.0, 0.0, 0.0, 0.0) (
            docs="""imu orientation"""
        )
        custom uint inputs:port = 5561 (
            docs="""server port"""
        )
        custom string inputs:serialNumber = "109999999" (
            docs="""Serial number of the stereo cam. Only used for virtual ZED X cameras, otherwise the serial number is automatically alocated"""
        )
        custom int inputs:simCameraModel = 4 (
            docs="""SDK MODEL code of the camera (as in SimCameraInfo::model). Set by the annotator via utils.get_sdk_model_id(); -1 selects the custom virtual-stereo path."""
        )
        custom int inputs:simLensType = 0 (
            docs="""Lens fitted to the camera, as SIM_LENS_TYPE: 0=Wide, 1=Narrow, 2=Fisheye. Set by the annotator via utils.get_sim_lens_type_id()."""
        )
        custom double inputs:simulationTime = 0.0 (
            docs="""simulation time"""
        )
        custom bool inputs:stream = false (
            docs="""stream"""
        )
        custom bool inputs:streamDepth = false (
            docs="""Enable Left+Depth streaming mode instead of Left+Right"""
        )
        custom double inputs:systemTime = 0.0 (
            docs="""system time"""
        )
        custom token inputs:transportLayerMode = "BOTH" (
            docs="""Communication protocol used to send data to the ZED SDK."""
        )
        custom uint inputs:width = 960 (
            docs="""Camera stream width in pixels."""
        )
        custom float inputs:zedSim2RealAeTarget = -1.0 (
            docs="""Override the sim2real AE set-point (median display luma 0..1). <=0 = calibrated default (~0.45). Raise to brighten the modeled image, e.g. to sit closer to the raw render's tone mapping."""
        )
        custom float inputs:zedSim2RealSceneLux = 0.0 (
            docs="""Absolute scene illuminance in lux for the sim2real's gain-ceiling AE and gain-coupled noise. 0 = bright scene"""
        )
    }
}
